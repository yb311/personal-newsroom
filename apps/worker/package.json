{
  "name": "@pnr/worker",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "dist/worker.cjs",
  "scripts": {
    "build": "esbuild src/worker.ts --bundle --platform=node --format=cjs --outfile=dist/worker.cjs --external:better-sqlite3 --external:sqlite-vec --external:rsshub"
  },
  "dependencies": {
    "@pnr/ai": "*",
    "@pnr/core": "*",
    "@pnr/feed": "*",
    "@pnr/generate": "*",
    "@pnr/reader": "*",
    "@pnr/recall": "*",
    "@pnr/store": "*",
    "@pnr/watch": "*"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
