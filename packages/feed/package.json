{
  "name": "@pnr/feed",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@pnr/core": "*",
    "@pnr/store": "*",
    "fast-xml-parser": "^5.11.1"
  },
  "//": "rsshub is deliberately NOT a dependency: it is ~415MB and ships as a separately downloaded pack (see src/rsshub-pack.ts). The adapter imports it lazily and works without it."
}
